export default function RequestsPage() {
  return (
    <div>
      <div className="mb-5 pb-3 border-b-2" style={{ borderColor: 'var(--primary)' }}>
        <div className="flex flex-col gap-0.5">
          <h1 className="text-[20px] font-bold">修正申請</h1>
          <span className="text-[10px] font-mono tracking-[0.16em]" style={{ color: 'var(--text-faint)' }}>CORRECTION REQUESTS</span>
        </div>
      </div>
      <p className="text-[13px]" style={{ color: 'var(--text-muted)' }}>実装予定</p>
    </div>
  )
}
